# Dependencies
node_modules/

# Cloned repos (will be cloned by clone-repo)
ecc/
9router/

# API keys (sensitive)
api-key.txt

# Session state (private, user-specific)
.opencode-session.json
.iteration.json
ai-notes.md

# Project memory (user-specific)
project-memory/

# Config backups
*.bak.*

# OS
.DS_Store
Thumbs.db

# .sync-state.json is NOT ignored (committed for zip backup)
