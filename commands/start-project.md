---
description: [COMBO] Setup project — preprocess + start-free + set-project + code-analyze + analyze-project + learn
type: combo
agent: build
---
# Start Project — Combo
Setup project baru dalam 1 command.
## Skills yang Diaktifkan
tdd-workflow, coding-standards, codebase-onboarding
## Instruksi
1. **`/go "setup project baru"`** — preprocess: detect stack, skill, feature, memory
2. `/start-free` — start 9Router + test model + apply config
3. `/set-project path` — set project path
4. `/code-analyze` — scan source → ai-notes.md
5. `/analyze-project` — deteksi stack + load skills
6. **`/learn "Project setup complete"`** — save ke memory + knowledge
## 🔴 Error Recovery
Jika gagal: `/reset-session` lalu mulai dari `/morning-routine`
## Task
$ARGUMENTS
