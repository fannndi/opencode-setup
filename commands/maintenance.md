---
description: [COMBO] Maintenance — preprocess + admin + quality-gate + reset-session + learn
type: combo
agent: build
---
# Maintenance
Update system + cek kesehatan — dengan enriched context.
## Skills yang Diaktifkan
verification-loop, strategic-compact, context-budget
## Instruksi
1. **`/go "maintenance"`**
2. `/admin` — update ECC + 9Router + doctor check
3. `/quality-gate` — verifikasi sistem masih sehat
4. `/reset-session` — reset session (opsional)
5. **`/learn "Maintenance done"`**
## Task
$ARGUMENTS
